'use strict';

describe('Directive: UserLogin', function () {

    // // load the app module
    // beforeEach(module('AllClearApp'));

    // var element, scope, elementScope;

    // beforeEach(inject(function ($rootScope, $compile) {
    //     scope   = $rootScope.$new();
    //     element = angular.element('<user-login></user-login>');
    //     element = $compile(element)(scope);

    //     scope.$digest();

    //     elementScope = element.children().scope().ctrl;

    // }));

    // it('should exist', inject(function ($compile) {

    //     expect(elementScope).toBeDefined();

    // }));

});